trigger:
  branches:
    include:
      - dev
      - prod

parameters:
  - name: BRANCH
    displayName: 'Branch'
    type: string
    default: prod
    values:
      - dev
      - prod

pool:
  name: vm-race-pool

jobs:
  - job: Deploy
    displayName: 'FE Deployment - race_ai'
    variables:
      DEPLOY_REQUESTED: 'false'
    strategy:
      matrix:
        Primary:
          AGENT_NAME: 'vm-race-un-p-01' # 👈 your primary agent name
        Backup:
          AGENT_NAME: 'vm-race-un-p-01-bck' # 👈 your backup agent name (update this)
    pool:
      name: vm-race-pool
      demands:
        - Agent.Name -equals $(AGENT_NAME) # pins each matrix leg to its VM

    steps:
      - checkout: self
        clean: 'true'

      - script: |
          echo "Build reason: $(Build.Reason)"

          if [ "$(Build.Reason)" = "Manual" ]; then
            echo "Manual trigger detected — skipping commit message check, proceeding with deployment..."
            echo "##vso[task.setvariable variable=DEPLOY_REQUESTED]true"
          else
            COMMIT_MSG=$(git log -1 --pretty=%B)
            echo "Commit message: $COMMIT_MSG"

            if [[ "$COMMIT_MSG" == deploy* ]]; then
              echo "Deploy keyword detected — proceeding..."
              echo "##vso[task.setvariable variable=DEPLOY_REQUESTED]true"
            else
              echo "Skipping deployment — commit message does not start with 'deploy'"
              echo "##vso[task.setvariable variable=DEPLOY_REQUESTED]false"
            fi
          fi
        displayName: 'Check Commit Message'

      - script: |
          set -e
          set -x
          echo "=== FE Deployment Started on $(AGENT_NAME): $(date) ==="

          TRIGGER_BRANCH="${BUILD_SOURCEBRANCH##refs/heads/}"
          if [ "$TRIGGER_BRANCH" = "dev" ] || [ "$TRIGGER_BRANCH" = "prod" ]; then
            BRANCH="$TRIGGER_BRANCH"
          else
            BRANCH="${{ parameters.BRANCH }}"
          fi

          echo "Deploying branch: $BRANCH on agent: $(AGENT_NAME)"

          if [ "$BRANCH" = "dev" ]; then
            cp /app/apps/assortment/rationalization/fc_app/assortment_l_fc_dev/Dockerfile $(Build.SourcesDirectory)/
            cp /app/apps/assortment/rationalization/fc_app/assortment_l_fc_dev/.env $(Build.SourcesDirectory)/
            IMAGE_NAME="assortment_r_fc_dev_1"
          else
            cp /app/apps/race-ai/fc_app/race_ai/Dockerfile $(Build.SourcesDirectory)/
            cp /app/apps/race-ai/fc_app/race_ai/.env $(Build.SourcesDirectory)/
            IMAGE_NAME="race_ai_prod_7"
          fi

          OLD_CONTAINER=$(docker ps -q --filter "ancestor=$IMAGE_NAME")
          if [ ! -z "$OLD_CONTAINER" ]; then
            echo "Stopping old container: $OLD_CONTAINER"
            docker stop $OLD_CONTAINER
            docker rm $OLD_CONTAINER
          fi

          docker build -t $IMAGE_NAME $(Build.SourcesDirectory)
          docker run --network=host -itd $IMAGE_NAME

          echo "=== FE Deployment Done on $(AGENT_NAME): $(date) ==="
        displayName: 'Build & Deploy'
        condition: and(succeeded(), eq(variables['DEPLOY_REQUESTED'], 'true'))
